#!/usr/bin/env sh
# Download Arch Linux User Repository (AUR) package sources to a directory.

if [ -n "$1" ]; then
	git clone "https://aur.archlinux.org/$1" "$1"
else
	echo "Please specify a package name" >&2
	exit 1
fi
