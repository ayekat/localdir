#!/usr/bin/env sh
# Searches for a Makefile in this or any parent directory and runs make.
# Written by ayekat on an annoying tuesday afternoon in april 2013.

while [ "$PWD" != / ]; do
	if [ -e Makefile ]; then
		make "$@"
		exit
	fi
	cd ..
done
echo No Makefile found! >&2
exit 1

