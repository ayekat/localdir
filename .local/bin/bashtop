#!/bin/bash
# Prints the [n] most used commands based on ~/.bash_history, where [n] is an
# optional argument (by default 10).

if [[ $1 == '' ]]; then
	LINES=10
else
	LINES=$1
fi

awk '{print $1}' < ~/.bash_history | sort | uniq -c | sort -rn | head -n $LINES

