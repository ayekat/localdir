#!/bin/sh

path="$XDG_CONFIG_HOME/dwb"
name='stylesheets'
extension='disabled'

if [ -e "$path/$name" ]; then
	mv "$path/$name" "$path/$name.$extension"
else
	mv "$path/$name.$extension" "$path/$name"
fi
