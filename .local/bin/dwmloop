#!/bin/sh
# executes dwm in a loop to allow on-the-fly restarts

while true; do
	if [ $HOSTNAME = 'phobia' ]; then
		sleep 2 && dzenstat start &
	else
		dzenstat start &
	fi
	dwm > /tmp/dwm-$USER.log
	dzenstat stop
done

