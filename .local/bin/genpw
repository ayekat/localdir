#!/bin/sh
# Creates a random sequence of characters.
# Taken from: http://www.cyberciti.biz/faq/linux-random-password-generator/

l=$1
[ -z "$l" ] && l=16
for i in $(seq 1 16); do
	tr -dc 'A-Za-z0-9_$' < /dev/urandom | head -c ${l}
	echo
done
