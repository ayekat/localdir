# GNU Screen configuration file.
#
# Hosteurope fuckery: since my server is a thousand years behind, I can't
# properly configure tmux, thus I need to fallback to screen.
# Here it is.
#
# Written by ayekat on a cloudy summer day in 2010.


# ------------------------------------------------------------------------------
# LOOK

# Enable 256 colours
# Found here: https://wiki.archlinux.org/index.php/Screen#Use_256_colors
attrcolor b ".I"
termcapinfo xterm "Co#256:AB=\E[48;5;%dm:AF=\E[38;5;%dm"
defbce on

# Draw a pretty statusline:
hardstatus alwayslastline "%{= yk} screen %{= Kw} %H %{= kK}  %-w%{+ .w}%{+b .y}%n%{-} %t%{-}%+w%=%{= Kw} ... %{= wK} %0c "

# ------------------------------------------------------------------------------
# FEEL

# Set escape character to Ctrl-b, as in tmux:
# github: should look like "^Bb"
escape "b"


# ------------------------------------------------------------------------------
# FIXES

# Hosteurope fuckery: misbehaving character encoding on my server:
defc1 off
defutf8 on

# Don't display the copyright splash screen everytime:
startup_message off

# Correctly erase output after closing ncurses application:
altscreen on

# Disable visual bell (at least a bit; screen is super annoying):
vbell off

